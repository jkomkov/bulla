# Bulla 0.49.4 — corporate identity

Current public organization copy uses Bulla Labs. Its legal operator is Bulla
Labs, Inc.; the owner confirmed this is the same corporation under its effective
new name. This is not an ownership transfer, governance change, or new authority.

The public package source is `jkomkov/bulla`, based on published 0.49.3 source
`70e603401d9876501064b3ed7e4d17838cb0adf7`. The allowed-difference manifest
protects runtime and specification bytes, permits the version declaration, and
enumerates documentation, descriptive metadata, tests, and release controls.
The historical 0.49.3 manifest remains unchanged, including its Windows repair.

No dependency, runtime behavior, receipt format, policy, or verification semantic
changes are included. New producer provenance and receipt hashes may change with
the package version. Historical records, compatibility identifiers, prior
releases, copyright notices, licenses, and maintainer roles remain unchanged.

Two role-separated R3 reviews and two consecutive clean release passes precede
exact-main preflight. Compatibility, reproducible builds, closed inventories,
installed-wheel tests, and two session-root lifecycles precede any release slot
or tag. Preparation consumes that successful preflight and dispatches trusted
publication of the frozen wheel and sdist without rebuilding. PyPI identities
and provenance must match before signed finalization. An occupied 0.49.4 identity
stops this plan; it does not authorize a different release or reuse of a slot.

The branding inventory separates current copy from retained historical evidence,
compatibility identifiers, and internal infrastructure. Existing old-domain
redirects remain operational. No public API or wire identifier is renamed.
